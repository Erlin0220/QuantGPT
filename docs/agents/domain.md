# Domain Docs

This repository uses a single-context domain documentation layout.

## Before exploring

- Read `CONTEXT.md` at the repository root when it exists.
- Read relevant decisions under `docs/adr/`.
- Use the glossary's canonical terminology in specs, tickets, tests, and implementation discussions.
- If an implementation proposal conflicts with an ADR, surface the conflict rather than silently replacing the decision.
